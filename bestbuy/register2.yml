info:
  name: register2
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/auth/register
  body:
    type: json
    data: |-
      {
        "name": "Ineza Orianne Caela",
        "email" : "caela@gmail.com",
        "password": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
